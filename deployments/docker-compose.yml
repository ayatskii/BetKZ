version: "3.9"

services:
  backend:
    build:
      context: ../backend
    container_name: betkz-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ../frontend
    container_name: betkz-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
